{
	"plugins": {
		"@release-it/bumper": {
			"in": [
				"src/chrome/manifest.json",
				"src/firefox/manifest.json"
			],
			"out": [
				"src/chrome/manifest.json",
				"src/firefox/manifest.json"
			]
		}
	},
	"git": {
		"requireBranch" : "main",
		"commitMessage": "Release v${version}"
	},
	"hooks": {
		"before:init": [ "git push", "pnpm lint-fix" ],
	    "after:bump": "pnpm auto-changelog -p",
	    "after:release": "Github action is now releasing: ${name} v${version} to ${repo.repository}. Check if all is ok 🌈🤖"
	},
	"github": {
		"release": false
	},
	"npm": {
		"release": false
	}
}